# P2Less — Project Status (goal / achieved / remaining)

**This is the one document to read first for "where are we."** Kept current every round and pushed immediately — not a snapshot that goes stale. Full reasoning, evidence, and history live in the linked docs below; this page states the current state in the fewest words that stay honest.

**On a new machine?** See [`README.md`](../README.md)'s "Lost your laptop? Start here" section — clone from GitHub, production is already live at Railway and needs no local setup, and real secrets come from `railway variables`, not from memory (`.env` is deliberately never committed).

**Worried something's broken, missing, or half-built?** [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) is the one current, evidence-backed list of every known gap across security, money/billing, data integrity, and missing CRUD operations — each with real status verified against actual code, not an old note. Check there first before assuming something is fine or reopening an investigation that's already been done.

---

## The goal

*"Connect your existing systems to P2Less, and P2Less gives your people one intelligent way to access information, perform actions, automate work, and communicate across those systems."*

P2Less is a universal digital access, automation, integration, AI, and communication layer — **augmentation, not replacement**. Organizations keep their existing systems (school management software, hospital records, payroll, retail order tracking, whatever they already run); P2Less connects to those systems and gives their people (staff and customers/parents/patients) one conversational way in, over WhatsApp today and other channels as they ship.

Six architectural rules held as fundamental throughout: never fabricate certainty (every fact tagged Known/Calculated/Configured/Generated/Unknown); four modes (Connect/Operate/Automate/Create); an authority gate before any autonomous action (can AI do this? is the user authorized? does it need confirmation? then execute); every action is a registered Capability with its own risk level and approval requirements, never freeform execution; cross-system data conflicts resolve by configured source-priority, never a guess; and "I cannot do that" / "I don't know" is treated as a trust feature, not a weakness to hide.

Full detail: [`VISION-UNIVERSAL-ACCESS-PLATFORM-2026-08-19.md`](VISION-UNIVERSAL-ACCESS-PLATFORM-2026-08-19.md).

---

## What's been achieved

Everything below is shipped, live-verified (not just typechecked), and deployed to production unless a line says otherwise.

**Foundation (Phases 1-3, 6)**: branch hierarchy + capability schema + provenance typing; branch-scoped routing/RBAC; the capability gate every action runs through; OpenAPI-driven connector drafting (paste a spec, review, go live) plus a platform-curated connector marketplace.

**Conversation engine**: grounded AI with 7-provider automatic failover (never fabricates — explicit Known/Generated distinction in the prompts themselves), full RBAC, hash-chained tamper-evident audit logs (per-tenant and platform-wide), OTP step-up for sensitive data with a 180-day re-verification staleness gate (both for single actions and bundled overviews), conversational CRUD (book/reschedule/cancel), workflow-engine primitive live on 9 of 9 quantified conversation states.

**Channels — live today**: WhatsApp (the original channel) and the embeddable website widget. **Built, needs one real-world step to prove the round-trip**: Facebook Messenger — now handles media attachments and postback buttons too (2026-08-24), not just text; a real Page is connected in production, still needs a Meta App Tester invite accepted to prove a real inbound round-trip — Telegram (needs a real `@BotFather` token), Email via Resend Inbound (needs the Resend dashboard configured). **Not started**: Instagram DMs (blocked on Meta App Review), WhatsApp self-service onboarding for other tenants (Phase 9, paused on a spare test number).

**Registration & data model**: `useCases`/`channelsNeeded` capture what an org actually wants at signup (not sector-only); capability-based dashboard navigation shows only what's relevant to what a tenant actually uses.

**Security hardening**: structural tenant isolation (an ORM-level Prisma extension auto-scoping all 27 tenant-scoped models, AsyncLocalStorage-backed) — now genuinely fail-closed in production, not just warn-only. 2026-08-24: found and fixed the REAL reason the fail-closed version broke production before — not "RSC doesn't propagate context" (that theory was wrong), but that a guard function setting context and then *returning* a value never survives, even in Route Handlers; the fix is guard-and-invoke wrapper functions (`withTenantUser`, `withAdminPermission`, etc.) instead of guard-then-continue. Every real dashboard and admin entry point converted and verified via direct reproduction under a real `next build && next start`. Deployed warn-only first, watched real production traffic for a clean window, then flipped `db.ts` back to a real throw — re-verified the same rigorous way before that final deploy too. See [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 1 for the full story. SSRF guard shared across every server-side fetch of a tenant-supplied URL, with IP-pinning closing a DNS-rebinding TOCTOU window (2026-08-24); `CREDENTIAL_KEY` rotated from a guessable placeholder to a real random key; trial-abuse hardening on `/onboard` (email canonicalization, signup rate-limiting, real phone OTP, admin anomaly alerts) — the card-on-file item was deliberately removed 2026-08-25, see the onboarding-restructure entry below; real WhatsApp Cloud API token-health monitoring (2026-08-24) — a silently revoked shared platform token now opens a real Incident instead of going undetected.

**Growth features**: auto-publish new products to Facebook/Instagram (zero ongoing human login), resume-on-refresh for every multi-step flow, real website-content crawling into draft FAQs.

**Billing lifecycle**: subscription cancellation shipped 2026-08-24, admin-only by explicit direction — immediate cutoff (never gated behind a payment), final cycle's usage billed and emailed as an invoice rather than pushed for in real time. Plan changes shipped 2026-08-25, downgrades admin-only and deferred to the next renewal (closes the "downgrade right before the bill" gaming risk). Self-service upgrade was rebuilt 2026-08-25 as a real invoice-centric paid-upgrade flow after a critical gap was found and fixed same-day (a real `Invoice` model, backend-computed proration of remaining plan value, atomic/idempotent payment settlement). **STK Push is deployed and backend-verified** (real Daraja sandbox call, real callback, exact settlement, production schema/routes/UI confirmed healthy) — **a real production live-money payment was deliberately never fired**, to avoid an uncontrolled real transaction; that distinction is recorded precisely so "backend-verified" is never later misread as "a real payment succeeded in production." **Paybill shipped the same day**, built on the identical invoice engine rather than a second payment system — real DB-backed invoice-reference matching (not fuzzy, not by amount/phone/customer), partial/multi-payment settlement via a real running total, and a concurrent-payment race (two simultaneous partial payments summing to the threshold) verified to settle exactly once. **Visa/Card via Stripe shipped 2026-08-25**, the third and last payment method on the same invoice engine — real hosted Stripe Checkout (zero PCI scope, no custom card form), the first webhook in this codebase to verify a cryptographic signature via Stripe's own SDK (a bad `Stripe-Signature` is a real `400`, never swallowed as `200`), KES's two-decimal handling confirmed live against a real Stripe test-mode Checkout page before any dependent code was written. Live-verified end to end for real — real login, real Stripe-hosted checkout, a real test-card payment (`4242 4242 4242 4242`, zero real money), real redirect back with an honest "confirming now" message never a premature "upgraded" claim; settlement itself confirmed via a self-signed webhook payload since Stripe's real servers can't reach an unexposed local dev box. **The real production webhook endpoint was registered and live-verified 2026-08-25** — a real Stripe-delivered, Stripe-signed `checkout.session.expired` event was confirmed processed successfully by the production route (`InboundEvent`: processed, 200, no error), proving the signature-verification path works end to end against Stripe's real infrastructure, not just self-signed payloads. Card stays gated off (`PaymentChannel.implemented` false) regardless — production's Stripe key is still test-mode, so a real customer's real card would fail rather than charge; going live needs a live-mode Stripe key pair plus a separate live-mode webhook registration, both still pending. **Invoice-aware manual reconciliation shipped the same day** — closes the last gap in the money-workflow redesign: when automatic invoice matching fails (typo'd Paybill reference, STK amount mismatch), an admin can now attach the evidence to the correct invoice through the same `settleInvoice()` authority, with a live search scoped to the admin's own tenant authorization, an advisory preview before confirming, and the write action independently re-verifying the invoice's tenant — a real cross-tenant bypass attempt was tested live and rejected. A genuine pre-existing bug was found and fixed in the same pass: the original tenant-only "unknown payment" resolution buttons were silently broken by the 2026-08-23/24 tenant-isolation hardening (`TenantContextMissingError`) — fixed, not just worked around. Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) items 6, 7, 8, and 9.

**Onboarding restructured 2026-08-25 — Phase 1 of a 4-phase initiative.** A full field-by-field census of `/onboard` found it conflating identity verification, product-interest collection, and channel connection into one flow — every signup got a `WhatsAppNumber` reserved automatically regardless of whether the org wanted WhatsApp, and a $0 Stripe card step ran before anyone had seen the product. Onboarding now creates identity + workspace only: org details, admin identity, phone OTP (verifying the *admin*, not a WhatsApp number), done — no card step at all, no channel of any kind auto-created. A real dependency this surfaced was fixed, not just described: the Meta Embedded Signup callback assumed a `WhatsAppNumber` row already existed from signup; it now creates one on demand instead, so "Connect WhatsApp" from the dashboard keeps working. Verified live end to end twice, confirmed via direct database query each time: zero `WhatsAppNumber`/`Channel` rows created by signup. Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 10.

**Phase 2 (the "Explore" hub) shipped the same day.** A one-time, sequential, skippable, revisitable post-signup wizard at `/explore` — use cases, then channels, then an optional free-text goal, one step at a time, GoLogin/Lovable-style. `Tenant.exploreCompletedAt` gates the first-login redirect; a persistent "Explore P2Less" nav link lets any tenant reopen it later with prior selections pre-filled. Live-verified: first login redirects to `/explore`, finishing it correctly surfaces the previously-hidden Commerce/Website-widget nav groups, a second login goes straight to `/dashboard`, and "Skip for now" leaves `useCases`/`channelsNeeded` honestly null rather than recording a fake empty interest. The same session also carried a landing-page copy/design pass (all em-dashes removed from user-facing copy, real channel-brand icons replacing letter monograms, two stale "$0 card" mentions corrected, staggered card animations). Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 11.

**Forgot-password shipped 2026-08-25** — none existed before. `/forgot-password` requests a reset link (rate-limited per-IP and per-email), `/reset-password?token=...` confirms it. Same hashed-single-use-token discipline as the phone OTP flow (`PasswordResetToken`, sha256'd, 30-minute expiry, never stores the plaintext); a successful reset invalidates every other outstanding reset token AND revokes every other active session for that user, then signs the user straight in. Deliberately identical response whether or not the email is registered, closing the same user-enumeration gap `loginAction` already avoids. Live-verified end to end via direct DB inspection: token created correctly, consumed exactly once (a second use of the same link is honestly rejected), password and session state updated as expected. Email delivery reuses the already-integrated Resend — real tenant delivery is blocked on the same sandbox-key limitation already tracked for every other outbound email this app sends (see item 3 below). The login/forgot-password/reset-password pages were also redesigned the same day (shared `AuthShell`, no more flat black panel, a quiet ambient background animation instead of loud foreground content, real Hamzone/version attribution). Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) items 12–13.

**Phase 3 (real channel-connection lifecycle) shipped 2026-08-26.** New `Channel.connectionStatus` (not_started/connecting/connected/needs_attention/failed), set explicitly by the three real connect flows (Messenger, Telegram, Email); WhatsApp keeps its own `verificationStatus` as source of truth, mapped into the same vocabulary at the presentation layer. `getChannelGaps()` cross-references a tenant's stated Explore interest against real connection state — a dashboard nudge card and an emphasized Channels-page empty state now actually point a tenant toward what they said they wanted but haven't connected, closing the gap Phase 2 left open (interest was collected but nothing ever acted on it). Live-verified end to end: a real gap correctly surfaces a WhatsApp-specific nudge, an unrelated tenant with no stated interest shows none. Boot-time backfill for pre-existing `Channel` rows verified locally before trusting it on deploy. Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 14.

**Phase 4 (interest-history tracking + admin Product Intelligence) shipped 2026-08-26 — closes the 4-phase onboarding-restructure initiative.** New `TenantInterestEvent` model records every individual value added/removed to a tenant's `useCases`/`channelsNeeded`, not just the overwritten snapshot — structured columns (not a JSON blob) so aggregate counts are plain `count()`/`groupBy()` queries. Both write sites (Settings, Explore) now diff old vs new and record real events; Explore's revisit path was corrected to fetch prior selections first so a genuine change on a second visit is recorded, not silently dropped. New `/admin/product-intelligence` page (own dedicated `product_intelligence.view` permission) shows live aggregate counts per use-case/channel, a use-case × channel cross-tab, a 14-day history trend chart, and a drill-down table linking to each tenant's existing detail page. Boot-time backfill gives pre-existing tenants a baseline history, verified locally (idempotent, confirmed 0 on a second pass) before trusting it on deploy. Live-verified end to end: a real Settings change and a real Explore revisit both recorded the correct events with the correct source and correctly ignored unchanged values; the admin dashboard's numbers matched a direct database query. Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 15.

**Prepaid billing redesign — in progress, 2026-08-25**: post-paid usage (billed at renewal, real risk of unpaid accumulation) replaced with two real prepaid KES balances per tenant (messages, AI understanding), checked *before* the real external cost, never after; Enterprise alone stays post-paid. Message-balance gate and a centralized AI-balance gate (inside the one real `callLLM()` choke point every AI-calling function shares — closes a real gap where three AI-calling functions besides `understand()` were found live-untracked) are both shipped and live-verified, including graceful no-disclosure degradation on exhaustion and a boot-time migration so subscriptions that predate this feature aren't cut off. Low-balance notifications shipped and live-verified — one combined SMS+email notification (never two) per crossing, plus a dashboard bell warning, both live-verified for correct threshold detection, once-per-crossing timing, and clearing after a top-up. **Not yet built**: the top-up flow and the `/onboard` trial-first flow changes. Full detail: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 5.

**Unofficial per-channel transport switcher — in progress, 2026-08-26, WhatsApp Stage 1 live-verified end to end.** Triggered by repeated Meta Business Verification document rejections (root cause found separately — wrong document types submitted for a check that needs one document showing both legal name and phone number). A deliberate, explicitly-discussed, informed decision to build a second connection path per channel that doesn't depend on Meta's registration at all — **every route here violates the platform's own Terms of Service and can be banned without warning**, recorded plainly rather than glossed over. WhatsApp's alternative (device-paired via QR code, Baileys) is built: new `WhatsAppNumber.transport` column, a connection-manager module, a `deliver()` branch, and dashboard controls (a "Connect via alternative" button, a per-number switch with a real breakdown-before-you-commit confirmation modal). `@whiskeysockets/baileys` was installed (this session's own sandbox denied installing it directly; the user ran the install themselves), then the module was rewritten against its real installed types, catching real mismatches `tsc` alone found (WhatsApp's migration toward opaque LIDs affecting how a paired number's phone number is read). One real Turbopack build issue was also found and fixed (`next.config.ts`'s `serverExternalPackages`, same fix already used for `pdfkit`/`pdf-parse`). **Genuinely live-verified**: clicking "Connect via alternative" produces a real WebSocket handshake with WhatsApp's own servers and a real, scannable QR code in the dashboard — confirmed via server logs, not assumed. Hamzone's real production number was never touched (the switch-confirmation modal was opened against it to check its copy, then deliberately cancelled). Full regression suite clean (74/74), `tsc --noEmit` clean against real types. **Still unverified**: actually scanning the QR with a phone through to a working message round-trip. Messenger and Instagram (Stages 2–3) not started — Instagram in particular needs a Python sidecar and faces active 2026 Meta DMCA enforcement. Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 16.

**Baileys transport tracking, differential billing, and platform kill switch — shipped 2026-08-26.** Built entirely on existing admin-configurable patterns, no new mechanisms: two new `PlatformSetting`s (`baileys_billing_active`, off by default; `baileys_discount_multiplier`, default 0.6×) editable on `/admin/billing` with the same save-toast every other pricing field has — Baileys messages are free-but-tracked until an admin turns billing on, then charged at a configurable discount off the normal conversation rate, AI charges untouched either way. A new `whatsapp_baileys` row in the existing `Integration` catalog gives `/admin/integrations` a real kill switch (its existing Enable/Disable control, no new UI) that both stops real sends (`transport.ts`) and hides both tenant-dashboard entry points to connect/switch to it. `/admin/tenants` and `/admin/tenants/[id]` now show which numbers are on Meta vs. the alternative. **Live-verified for real**: the exact debit math was proven through a genuine signed HTTP webhook request exercising the real code path (100 KES → 98.8 with billing on, unchanged with it off, `UsageEvent.transport` recorded either way) — not assumed from reading the arithmetic. The kill switch was verified live in the browser: disabling it made both dashboard entry points disappear immediately, re-enabling brought them back. All test data cleaned up afterward; Hamzone's real number/balance untouched. Full regression suite clean (74/74). Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 17.

**Real, admin-editable AI provider keys + standalone WhatsApp disconnect — shipped 2026-08-26.** Triggered by Groq hitting its daily quota mid-session (an 85-second reply cycling through dead providers). `/admin/ai` now has a real "Add key" flow per provider — numbered "Key 1"/"Key 2"/... with real tracked tokens-used and an admin-entered starting-balance estimate (no provider exposes a live balance API, so remaining is an honest bookkeeping figure). Wires up `IntegrationCredential`, previously real but completely unused scaffolding, as a runtime source for the first time. A real architectural fork was decided mid-build: rather than making `getProviderKeys()` async (which would have rippled into 20+ call sites in `conversation.ts`, the platform's most critical file), a `globalThis`-backed in-memory cache keeps everything synchronous — zero changes to the conversation pipeline. Separately, direct request surfaced a real gap: `/dashboard/channels` only offered "Switch to [other transport]," no plain disconnect. Turned out `WhatsAppNumber.status` was already the real routing gate `conversation.ts` checks — just needed a UI control. Live-verified for real: added and revoked a real key via the UI (confirmed via direct DB query and the admin activity feed), and confirmed disconnecting a real number genuinely stops it — a signed webhook POST to a disabled number created zero Contact/Conversation rows, not just a cosmetic status flip. Full regression suite clean (74/74). Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 18.

**P2Less's own tenant provisioned for internal training — shipped 2026-08-26.** Triggered by wanting to run internal AI-quality training on P2Less's own account (real WhatsApp number, shared publicly, usage tracked but never gated on payment). The "P2Less" tenant already existed (30 real FAQs, 229 messages of organic landing-page-widget history) but was suspended, with no logins and no subscription — it had never been through a real signup. `scripts/prod-start.mjs` now idempotently reactivates it, seeds its owner Role, creates a real login (`admin@p2less.internal`), and assigns a new dedicated `"internal"` Plan (`priceMonthly: 0`, `postpaidUsage: true`, `active: false` so it's never customer-selectable) — reusing the exact prepaid-exemption mechanism Enterprise already relies on (`isGateExempt()`), not new billing logic, and kept as its own plan rather than reusing Enterprise so it doesn't skew real business metrics. Usage still meters normally; only the top-up requirement is skipped. Live-verified twice for real idempotency, then logged into for real, confirming the dashboard, plan label, and full pre-existing conversation history. **Not yet built**: connecting a real WhatsApp number to this tenant, and open enrollment for training (today's Training Sessions feature only gates pre-enrolled numbers). Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 19.

**Open-enrollment training + camera-free WhatsApp pairing — shipped 2026-08-26.** Two direct blockers on P2Less's own open training run: Training Sessions only gated pre-enrolled numbers (the ask was "anyone who texts it, no pre-enrollment"), and the phone meant to pair via the unofficial transport has both cameras broken. `TrainingSession.openEnrollment` is now a per-session checkbox — an active open session auto-enrolls a contact's first message into the same atomic, capped counting logic every enrolled participant already uses; the old allowlist/join-code modes are untouched. Its share link is a plain `wa.me/<number>` with nothing pre-filled — opens a chat, sends nothing, enrolls nobody until the person actually types. Separately, `/dashboard/channels`'s "Connect via alternative" gained an optional phone-number field that requests a real typed Baileys pairing code instead of a QR scan — a real live bug was caught mid-build (`requestPairingCode()` called before the socket's handshake finishes throws "Connection Closed") and fixed with a bounded retry. Live-verified for real: a genuine inbound message from an unenrolled number correctly auto-enrolled (confirmed via direct DB query, then cleaned up); the pairing-code field and retry logic both exercised live. Full completion of a real phone-number pairing couldn't be confirmed from this sandbox (outbound WebSocket access to WhatsApp's servers appears blocked here, unlike production) — needs confirming there. Regression suite 73/74 (one pre-existing, unrelated date-formatting flake). Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 20.

**Training-priority AI pause + Baileys blue ticks — shipped 2026-08-26.** Once P2Less's own number was actually receiving real traffic, two direct needs surfaced: training shouldn't compete with real tenants for shared AI quota, and the connected number never showed blue ticks. `/admin/ai` now has a "Training-priority mode" card — pick a tenant, pause AI for everyone else with one click, restore with one click — enforced at the single `callLLM()` choke point the prepaid gate already uses, same graceful-degradation shape as every other AI gate. Baileys now sends real read receipts (`sock.readMessages`) alongside the typing indicator built earlier the same session. Live-verified both directions of the pause (blocked vs. exempted, confirmed via `AiRequestLog` row counts) against the real dev server. Full regression suite 74/74 clean on the second pass (one AI-response-wording flake on the first pass, confirmed unrelated to this diff by reading the failing test's fixture setup). Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 22.

**Image vision + full media parity on the Baileys transport — shipped 2026-08-26.** Confirmed two real gaps while checking "can it listen to voice notes and read images": no tool anywhere handled an image at all (document-intel.ts's own comment already admitted "if it's a scanned image, I can't read that yet"), and the unofficial (Baileys) transport only ever extracted plain text — voice notes, photos, and documents sent there were silently dropped, even though the official Meta transport already handles all three. New `analyzeImage()` plus a new `image-vision` tool in the existing dispatcher closes the image gap for every channel; Baileys' inbound handler now branches on image/audio/document exactly like the Meta webhook route already does. Live end-to-end verification surfaced a real, separate bug along the way: the STT-tuned Gemini model was timing out outright from production while the regular chat model worked fine — traced only because a genuine platform-wide silent-failure gap in `fetchT()` (every AI network error swallowed with zero logging, since the function was first written) got fixed in the same round. Voice/image now fall back to the working chat model instead of going dark. Three separate live production tests after the fix all returned real, accurate Gemini responses; full regression suite 74/74 clean throughout. Voice-note *replies* (TTS) deliberately sequenced next, pending an ffmpeg infra addition. Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) items 23–24.

**Voice-note replies (TTS) + two real bugs found building it — shipped and live-verified end to end 2026-08-26.** The immediate follow-up to voice/image listening: `ffmpeg` added to the Railway build (via `railpack.json` — the first `nixpacks.toml` attempt silently did nothing, since this project actually builds with Railpack, not Nixpacks; confirmed present in production afterward), then a full Gemini-TTS → WAV → Opus/OGG pipeline so a reply to a voice note comes back as a real WhatsApp voice note, mirroring the sender. Building the reply-delivery wiring surfaced two real, previously-live bugs: **every reply on a Baileys-connected number was being sent twice** (a manual re-send loop on top of the delivery path `deliver()` already used internally — undetected because every test this session used the webchat channel, which never reaches that code), and **documents were never actually deliverable on the unofficial transport at all** (no document param existed on the send function). Both fixed as part of this same round. Live-verified against production's real Gemini + real ffmpeg: a genuine, valid Opus/OGG file (confirmed via its real "OggS" container magic bytes) produced end to end, not just "no error thrown." `tsc` clean, full regression suite 74/74. Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 25.

**Channels page clarity + a real dead-session recovery gap — shipped 2026-08-26.** Direct report: two same-colored badges made it genuinely hard to tell if WhatsApp was connected, and "Connect via Meta" stayed clickable even while already connected via the alternative transport, implying a second connection was one click away. Investigating surfaced a real bug: `whatsappConnectionStatus()` collapsed "pending" and "failed" into the same green badge a healthy number gets. Both fixed — one combined, unambiguous status line per number, and the top connect buttons now disappear once a number exists (the card's own `Switch to Meta`/`Switch to alternative` is the only way to change transport). Diagnosing a real stuck production connection along the way surfaced a second gap: no way existed to recover a WhatsApp-side session close that leaves auth credentials permanently unable to resume — new "Forget & pair again" control forces a genuinely fresh pairing instead of an endless doomed retry. `tsc` clean, full regression suite 74/74 (after ruling out 3 unrelated AI-phrasing flakes via a clean re-run). Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 25.

**QR/pairing-code modal disappearing instead of opening — fixed 2026-08-27.** Direct report after actually using "Forget & pair again" for real: "the modal window disapper nothing is shown." Root cause: both `forgetAndRepairWhatsAppNumberAction` and `startWhatsAppUnofficialConnectAction` flip `WhatsAppNumber.status` back to `"active"` as part of restarting the connection, and two client components had their QR/pairing-code modal living inside a JSX branch keyed off that exact same `status` prop — the instant the DB write landed and the page revalidated, React swapped branches and tore the modal down at the moment it was supposed to open. Fixed by making both modals render unconditionally, never inside a branch the same action's own success also flips. Live-verified twice against a real tenant login: "Forget & pair again" now correctly keeps the QR modal open, and a fresh "Connect via alternative" with a typed phone number (the no-camera path) correctly renders and keeps showing a real server-generated pairing code. Separately confirmed (not a regression from this fix): this local dev sandbox's own path to WhatsApp's real servers is flaky — a live connect attempt can fail before ever reaching the pairing-code step — a pre-existing, already-documented environment limitation; full live pairing still needs confirming against production. `tsc` clean. Full story: [`GAP-REGISTER-2026-08-24.md`](GAP-REGISTER-2026-08-24.md) item 27.

**Full detail and evidence for every item above**: [`ROADMAP-UNIVERSAL-PLATFORM-2026-08-19.md`](ROADMAP-UNIVERSAL-PLATFORM-2026-08-19.md) (the phase-by-phase build log) and [`OPERATIONS-GUIDE-2026-08-23.md`](OPERATIONS-GUIDE-2026-08-23.md) (security/ops hardening rounds, numbered §1-§65+).

---

## What remains

### Blocked on an external provider or a decision only you can make — not more code

16 items now (items #1 and #5 done, see below), each with exact next steps, every one re-verified against live evidence 2026-08-24: [`EXTERNAL-REGISTRATIONS-CHECKLIST-2026-08-24.md`](EXTERNAL-REGISTRATIONS-CHECKLIST-2026-08-24.md). Fastest to close of what's left: a real Telegram bot token (minutes, self-service) and the Resend Inbound dashboard setup (~15 min, self-service).

✅ **The urgent Access/Tech-Provider warning found during the audit is resolved (pending Meta's review).** WhatsApp Business Verification (item #1) — legal name, address, phone, Tax ID, Primary Page, and domain verification — was fully completed and submitted 2026-08-24, walked through live end-to-end. Meta's own confirmation: "In review... about 2 business days." Once it clears, App Review (#3) and Instagram App Review (#7) become the next real steps.

**WhatsApp access-token health monitoring (item #5) is done** — shipped 2026-08-24, see Operations Guide §66.

### Genuine, unblocked engineering work still open

- **Public Feedback / Quality Centre** — Phase A (support-ticket triage dashboard) and now Phase B's invitation toggle (2026-08-24, admin-controlled, default off) are shipped. The three open design questions blocking Phase B are resolved — see `PUBLIC-FEEDBACK-QUALITY-CENTRE-2026-08-23.md`'s "Open questions" section. **Decision changed 2026-09-02**: the `TestExercise`/`TestCase`/`Finding`/`AssuranceReport` layer is no longer something P2Less builds itself — that's now the canonical data model of a separate sibling repo, [`hamzone-ai-training`](https://github.com/hamisisep-maker/hamzone-ai-training) (Hamzone Technologies' AI Training & Evaluation platform, P2Less as its first client). P2Less's own remaining work is just the receiving side: `POST /api/training/findings` to accept a validated finding and file it into `/admin/quality`, plus `POST /api/training/evaluate` for that platform to actually run test inputs through the real pipeline. Neither route exists yet. See this repo's own README ("This is one of two Hamzone Technologies repositories") for the full contract.
- **Phase 5 workflow engine** — 4 flows (`awaiting_otp`, `awaiting_identify`, `awaiting_cv_details`, `awaiting_delivery_feedback`) were deliberately left bespoke rather than migrated to the generic engine; may stay that way permanently.
- **`resolveFieldConflict()`** (Phase 4) — built and tested, zero live callers yet; no tenant has two connectors to the same external system to trigger a real conflict.

### Deliberately unscoped, future-strategic — no work started, and shouldn't be until there's a real need

Mode 2 outbound/proactive messaging (marketing, notifications, follow-ups to known contacts) — **now has a real scope written**: [`OUTBOUND-MARKETING-MESSAGING-SCOPE-2026-08-27.md`](OUTBOUND-MARKETING-MESSAGING-SCOPE-2026-08-27.md), which found this needs consent tracking (doesn't exist), real Meta message-template integration (doesn't exist), a new campaign data model, and batch-aware billing/rate-limiting, built in that order, Meta-official-transport only — not a quick add on top of the current reply-only pipeline; the Public Social Agent (auto-replying to public posts/comments — "Grok-on-X" style); social-media connectors, OCR/document ingestion beyond PDF, research/plagiarism tooling (Phase 7's unscoped remainder); X/Twitter as a channel (viable but costs money both directions — $0.025/round-trip); Voice/IVR (real and buildable, not started); TikTok (deferred). LinkedIn was evaluated and **rejected** — genuinely incompatible with P2Less's auto-reply model under LinkedIn's own policy.

---

*Last updated: 2026-09-02 (Email channel — the last item on the external-registrations fast-track — is now fully live-verified end to end: real Resend Inbound DNS (MX/DKIM/CNAME on `in.hamzonetechnologies.com`), real webhook delivery, a real inbound email correctly triggering a real grounded reply. Three real bugs found and fixed along the way: `RESEND_API_KEY` was a send-only restricted key silently failing every body-fetch call — the actual root cause of the channel doing nothing despite the webhook itself succeeding; `processEmail()`'s fire-and-forget path had zero error logging on any failure or early-return, making this invisible until diagnostic logging was added; and the outbound HTML template rendered every blank line in a reply as its own empty paragraph, stacking margins into the oversized gaps seen live. Also fixed a duplicate identity-reminder line (the AI's own reply already asked for the Employee ID in its own words; the hardcoded reminder then appended a second, near-identical ask) and corrected the platform-attribution wording to lead with Hamzone Technologies rather than naming its founder by default. Separately: [`hamzone-ai-training`](https://github.com/hamisisep-maker/hamzone-ai-training) — a new, deliberately separate Hamzone Technologies repository (AI Training & Evaluation platform; paid workers/reviewers test and harden AI systems; P2Less is Client #1) — was scoped and scaffolded this round. The only work item this creates on P2Less's side is the two integration routes noted above ("Genuine, unblocked engineering work still open"); neither is built yet, and nothing else in this codebase needs to change for that project to exist.)

*Previously — 2026-08-27: Video understanding added — a sent video is now downloaded, watched, and described the same way a photo or voice note already is, on both WhatsApp transports and on Messenger; found and fixed a stale capability gate that was incorrectly telling the AI Messenger couldn't receive files at all, when its webhook already handled them. Real live/video calling remains unbuilt and is not unlocked by this — no channel P2Less integrates with exposes any API for a bot to answer or join a call. See GAP-REGISTER item 33. Earlier the same day: Free is now a real 7-day trial — genuinely expires, then blocks with a real gate, not just cosmetically — after discovering it had been a permanent free tier despite being modeled as a trial; added Starter to the public pricing page and fixed Professional/Business's advertised message caps not being enforced; fixed a real gap where the message top-up only funded half of what a reply needs — one combined slider now correctly funds both the message and AI-understanding balances; added "back" navigation to the exhaustion modal's plan/top-up steps. On top of the same-day usage/balance visibility + exhaustion-modal work, the Free plan limits fix, P2Less's own FAQ fix, the greeting-ordering fix, the landing mockup's image/voice scene, the QR/pairing-code modal fix, Channels page clarity + "Forget & pair again," and voice-note replies, all shipped 2026-08-26/27). Update this doc (and push it) in the same round as any change that ships, closes an external item, or surfaces a new gap — don't let it go stale.*
